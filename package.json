{
  "name": "infra-gate0-harness",
  "type": "module",
  "scripts": {
    "benchmark": "tsx harness.ts",
    "typecheck": "tsc --noEmit",
    "upload:checkpoint": "tsx scripts/upload_checkpoint.ts",
    "docker:publish": "tsx scripts/publish_image.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1134.0",
    "@aws-sdk/lib-storage": "^3.1134.0",
    "dotenv": "^17.4.2"
  }
}
